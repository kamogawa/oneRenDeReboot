import * as actionTypes from '../actionTypes';

export const searchUnitName = (keyword) => ({
  type: actionTypes.SEARCH_UNIT_NAME,
  keyword: keyword
});

export const SEARCH_UNIT_PROFILE = (profile) => ({
  type: actionTypes.SEARCH_UNIT_NAME
});